@model IEnumerable<ODHDEVELOPERS.Models.DWAccList>
@using ODHDEVELOPERS.Models
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewBag.Title = "AllSavingaccount";
    var a = 0;

    UsersContext db = new UsersContext();
    var mr = db.Members.Single(m => m.Id == 1);
    
}

<div class="page-content">
      
                <div class="container">
                    <div class="page-toolbar">
                        
                        <div class="page-toolbar-block">
                            <div class="page-toolbar-title">Account List</div>
                            <div class="page-toolbar-subtitle"></div>
                        </div>
                        
                        <div class="page-toolbar-block pull-right">
                            
                        </div>           
                        
                    </div>

                     <div class="row" id="hide">
                        <div class="col-md-6" style="width:98%">

                   

  
                                          
<table border="1" style="margin-left:1%; margin-right:1px; margin-bottom:30px;width:98%;">
    @using (Html.BeginForm())
                {
                    foreach (var item in Model)
                    {
                        if (a == 0)
                        {
                              <tr><th colspan="3" ></th>
                                <th colspan="3" ><b>Account List</b></th>
                                <th colspan="3" ><b>@DateTime.Now.ToString("dd/MM/yyyy")</b></th>
                            </tr>
    <tr>
        <th style="width:5%;text-align:center;">
           <label id="sn"><b>S.N.</b></label>
            </th>
        <th style="width:20%;text-align:center;">
           <label id="newid"><b>Account No.</b></label>
            </th>
        
        <th style="width:20%;text-align:center;">
           <label id="introducerid"><b>Name</b></label>
        </th>
            
        <th style="width:10%;text-align:center;">
           <label id="position"><b>Address</b></label>
        </th>
             
       
         <th style="width:10%;text-align:center;">
           <label id="pay"><b>Mobile </b></label>
        </th>

         <th style="width:10%;text-align:center;">
        <label id="email"><b>Nominee's Name</b></label>
        </th>

         <th style="width:10%;text-align:center;">
           <label id="nage"><b>Nominee's Age </b></label>
        </th>

        <th style="width:10%;text-align:center;">
           <label id="nrel"><b>Nominee's Relation</b></label>
        </th>

        <th style="width:10%;text-align:center;">
           <label id="dt"><b>Date of Creation </b></label>
        </th>
      
    </tr>
    }
                        a = a + 1;


    <tr style="text-align:center;">
        <td>
            @a
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.accountno)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        
      
        <td>
            @Html.DisplayFor(modelItem => item.maddress)
        </td>

          <td>
            @Html.DisplayFor(modelItem => item.Mobileno)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.nomineename)
        </td>

         <td>
            @Html.DisplayFor(modelItem => item.nomage)
        </td>

           <td>
            @Html.DisplayFor(modelItem => item.nomrel)
        </td>

         <td>
             @Convert.ToDateTime(item.formdate).ToString("dd/MM/yyyy")
           
        </td>
       
             
    </tr>
                   
    }

    }
    
    
   
</table>
                            @if(a>0)
                            {
                                <a href="@Url.Action("Printallsavingaccount")" target="_blank">Get Report In PDF</a>
                            }
           </div>
                         </div>
                     
                    </div>
    </div>
